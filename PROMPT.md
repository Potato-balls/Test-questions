# AI 辅助开发提示词

## 任务描述

还原左侧视觉稿为一个移动端地址列表组件。

## 核心需求

### 第三行重点实现（最关键）

**标签布局规则：**
1. 标签内的字数是不固定的（中文/英文混合，如"常用"、"公司"、"距离最近"、"父母家"）
2. 标签可以放在第一行开头，也可以放在第二行作为结尾
3. 如果标签在第二行作为结尾，标签最宽只能占 50% 宽度
4. 地址最多显示两行

### 多端兼容

- React Native
- 微信小程序
- H5
- 支付宝小程序
- 抖音小程序

最终代码在以上五端的表达要尽可能一致。

如果时间不足以完成五端适配，至少完成其中 2 端。

## 技术方案

- 使用 Taro 框架（最新稳定版本）实现多端适配
- 使用 React 18 + TypeScript
- 使用 SCSS 编写样式
- 实现智能标签分配算法

## 设计决策

1. **框架选择**: Taro 4.x 是目前最成熟的多端 React 框架，支持五端编译
2. **样式方案**: SCSS + Taro 的 pxtransform 自动适配不同屏幕尺寸
3. **标签算法**: 根据字符数估算宽度，自动判断标签应该放在第一行还是第二行

## 关键代码逻辑

```typescript
// 智能标签分配算法
const estimateWidth = (tag: Tag): number => {
  const charCount = tag.label.length;
  if (/[一-龥]/.test(tag.label)) {
    return charCount * 20; // 中文字符
  }
  return charCount * 12; // 英文字符
};

// 第二行标签总宽度不能超过 50%
const MAX_SECOND_LINE_WIDTH = 187; // 375px * 50%
```

## 后续优化建议

1. 可以使用 Taro.useLayoutEffect 获取真实 DOM 宽度进行精确分配
2. 可添加更多标签类型支持
3. 可添加虚拟列表优化长列表性能
4. 可添加单元测试覆盖边界情况
